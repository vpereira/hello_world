workflow:
  steps:
    - branch_package:
        source_project: home:Admin
        source_package: hello_world
        target_project: home:Admin
    - set_flags:
        flags:
          - type: debuginfo
            status: enable
            project: home:Admin
          - type: publish
            status: enable
            project: home:Admin
