test_build:
  steps:
    - branch_package
        source_project: home:vpereirabr
        source_package: hello_world_multibuild 
        target_project: home:vpereirabr:PR
  filters:
    event: pull_request
rebuild_master:
  steps:
    - rebuild_package
        project: home:vpereirabr
        package: hello_world_multibuild
  filters:
    event: push
    branches:
      only:
        - master
