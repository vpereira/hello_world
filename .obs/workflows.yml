workflow1:
  steps:
    - branch_package:
        source_project: home:Admin
        source_package: hello_world

workflow2:
  steps:
    - branch_package:
        source_project: home:Admin
        source_package: hello
